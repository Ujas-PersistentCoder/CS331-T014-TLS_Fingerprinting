Create a new branch for the TLS-Fingerprinting git repository. Name it `python-oneshot`. Create a worktree for this branch as a sibling folder to [TLS-Fingerprinting](directory;file:///Users/pranjal/Projects/CS%20331%20CN%202026/TLS-Fingerprinting) . We will use this worktree to essentially "oneshot" most of the project using AI-driven development (basically, YOU!)

Let me know once the setup for the new branch is done

Ran command: `git worktree add -b python-oneshot ../python-oneshot`

The setup for the new branch is complete! 

I've created the `python-oneshot` branch and checked it out into a new worktree at `../python-oneshot` (a sibling directory to `TLS-Fingerprinting`). 

We are ready to start the AI-driven development. What would you like to tackle first?